{
  "name": "cvf-control-plane-foundation",
  "version": "0.1.0",
  "description": "CVF Control Plane Foundation - coordination package for control-plane shell surfaces",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "experiment:g2-topology": "vite-node scripts/run-runtime-topology-experiment.ts"
  },
  "keywords": ["cvf", "control-plane", "coordination-package", "foundation"],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}

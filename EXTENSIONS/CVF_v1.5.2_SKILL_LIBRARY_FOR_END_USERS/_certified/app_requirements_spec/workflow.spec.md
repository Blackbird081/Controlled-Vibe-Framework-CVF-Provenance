# Workflow Spec - App Requirements Spec

## Input Contract
Required inputs: app_goal, users, core_workflows, constraints, success_criteria. Inputs must describe a bounded product or internal-tool idea and must not include raw credentials, production secrets, or requests to deploy code.

## Output Contract
Expected output sections: Product scope, User workflows, Functional requirements, Acceptance criteria. Output is a planning handoff, not executable code or deployment approval, and must preserve governance receipt evidence.

## Deterministic Fixture Path
tests/fixtures/workflow/app_requirements_spec.fixture.json

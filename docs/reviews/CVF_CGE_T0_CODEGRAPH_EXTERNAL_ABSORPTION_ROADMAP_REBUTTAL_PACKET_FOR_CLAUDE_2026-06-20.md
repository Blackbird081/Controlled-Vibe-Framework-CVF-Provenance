# CVF CGE-T0 CodeGraph External Absorption Roadmap Rebuttal Packet For Claude

Memory class: FULL_RECORD

Status: DRAFT_FOR_CLAUDE_REBUTTAL

docType: review_context

Date: 2026-06-20

For: Claude, acting as adversarial reviewer only

Author: Codex

External absorption review: NOT_REQUIRED

## Purpose

Review the proposed CodeGraph External Absorption (`CGE`) roadmap before CVF
opens any GC-018 or work order.

Claude must challenge whether the roadmap correctly applies CVF's external
knowledge absorption chain to:

- upstream `colbymchenry/codegraph`;
- the local external-agent package under `CodeGraph/CVF_Code_Intelligence_Capability/`;
- existing CVF graph, memory, knowledge, and legacy-absorption evidence;
- the parked `ACE-R1` Agent Coding Evidence Replay Roadmap.

This packet asks for rebuttal only. It does not authorize implementation,
runtime/source edits, CodeGraph install, MCP wiring, daemon/watchers,
public-sync, provider/live proof, benchmark claims, freeze action, or universal
governed-coding-control claims.

## Scope / Target / Owner Boundary

Target: a bounded external-repo/copied-folder absorption roadmap critique for
CodeGraph-style code intelligence.

Owner boundary: this packet owns review context only. It does not make
CodeGraph, the local `CodeGraph/` folder, Claude output, or upstream benchmark
claims canonical CVF authority. Accepted content must return through CVF-owned
reviews, roadmaps, work orders, references, or completion packets before action.

Allowed in this packet:

- ask Claude to challenge source coverage, sequence, boundaries, and missing
  legacy/CVF graph evidence;
- cite public upstream CodeGraph observations as external input;
- cite local `CodeGraph/` files as advisory package evidence;
- cite governed CVF graph/legacy closure artifacts as current authority;
- request accept/adapt/defer/reject/block disposition corrections.

Forbidden in this packet:

- CodeGraph install, `codegraph init`, `codegraph install`, npm install, MCP
  wiring, watcher/daemon behavior, `.codegraph/` index creation, or runtime
  graph execution;
- source/runtime/test edits;
- provider/live proof, benchmark execution, public-sync, or push;
- public, production, release, readiness, or universal governed-coding claims;
- reopening `ACE-R1`.

## Target / Source

Target review output:
Claude's adversarial decision on whether CGE-T1 should be opened later,
rewritten, or blocked.

Primary source inputs:

- upstream CodeGraph public repository:
  `https://github.com/colbymchenry/codegraph`;
- local external-agent package:
  `CodeGraph/CVF_Code_Intelligence_Capability/`;
- current CVF graph source:
  `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/knowledge/graph/`;
- governed CVF graph/legacy closure artifacts listed in
  `## Current CVF Graph And Legacy-Relevant Authority Surfaces`;
- external-agent review and absorption standards listed in
  `## External Knowledge Intake Routing`.

## Scope / Methodology

Methodology expected from Claude:

1. Treat this packet as a review-context packet, not a work order.
2. Verify the listed source surfaces that are available in the local repo.
3. Compare upstream/local CodeGraph value against current CVF graph closures.
4. Identify missing legacy/KGR/MKG/AIF/PBR/N6/LHW13 coverage if the listed
   governed artifacts are insufficient.
5. Return a disposition and correction table without editing files or running
   install/runtime/provider/public operations.

Out of scope for Claude: broad raw legacy scanning unless returned as
`REWRITE_REQUIRED`, and all implementation/runtime/public activities listed in
the claim boundary.

## Findings / Position

Codex initial position for rebuttal:

`CGE-T1` is likely the correct next governed step, but only as a
doctrine/governance-first absorption triage matrix. It must not copy the local
package directly or install upstream CodeGraph.

Known blocker to challenge:

- local package `GraphReceiptService` can compute `freezeAllowed=true` from
  graph freshness/confidence alone, while the package protocol and CVF boundary
  state that graph receipt alone cannot authorize Freeze.

Known design risk:

- CVF already has a graph foundation, optional SQLite persistence, advisory
  `graph_search`, and graph authority boundaries. A CGE roadmap that creates a
  parallel graph subsystem would be wrong.

## Risk / Corrective Action

Risk level: R1 planning/review risk only.

Corrective action if Claude finds blockers:

- return `REWRITE_REQUIRED` with exact missing source, collision, or boundary
  defect;
- do not implement or patch;
- Codex must classify Claude's return through the external-agent finding
  absorption workflow before opening CGE-T1.

## Startup And Current Boundary

Current session mode:
`gkf_t1_governance_kernel_freeze_readiness_claim_boundary_audit_closed_next_foundation_ready`.

Active handoff:
`AGENT_HANDOFF_V20_2026-06-19.md`.

Next allowed move from current front door: select the next high-value
foundation lane with fresh GC-018 and source verification.

Operator direction for this packet:

- `ACE-R1` remains valuable but is parked for later.
- Apply the existing external-knowledge absorption rules now.
- Combine upstream CodeGraph audit, local `CodeGraph/` package knowledge, and
  relevant legacy/CVF graph knowledge before deciding whether to proceed.
- Ask Claude to rebut the roadmap before implementation.

## CVF Source-Of-Truth Statement

CVF remains the source of truth.

External repositories, copied folders, local external-agent packages, public
README claims, provider-local memory, and chat summaries are advisory input
only until CVF classifies them, maps them to owner surfaces, dispositiones
their value, and promotes accepted content through governed CVF artifacts.

## External Knowledge Intake Routing

| Field | Value |
| --- | --- |
| Chain map | `docs/reference/external_agent_review/CVF_EXTERNAL_KNOWLEDGE_ABSORPTION_CHAIN_MAP.md` |
| Input type | External repo or copied folder |
| Chain map route | External/corpus/repo input -> input router -> blind-spot and prior absorption evidence -> owner-surface map -> accept/adapt/defer/reject/block -> future GC-018/work order only if implementation is needed |
| Owner surface | Existing CVF Memory/Knowledge Plane, Learning Plane graph foundation, Context Builder, Work Order, Review, Freeze evidence, and External Agent Review workflow |
| Disposition | `PACKET_READY_FOR_REBUTTAL` if Claude finds no context blocker; otherwise `REWRITE_REQUIRED` |
| Matching local-view guard | `governance/compat/check_external_knowledge_intake_routing.py`; `governance/compat/check_external_agent_absorption_table.py` |
| Claim boundary | Review-context only; no implementation, install, runtime, MCP, public-sync, provider/live, benchmark, readiness, or universal control claim |

## Public / Private Boundary

| Surface | Disposition |
| --- | --- |
| Upstream CodeGraph public repo | Public external input. Claude may use it only as external reference, not CVF authority. |
| Local `CodeGraph/` folder | Operator-provided copied external-agent package. Treat as advisory package, not CVF source. |
| CVF current graph source under `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/` | CVF-governed source authority for current implementation facts. |
| Governed CVF reviews/reference packets listed below | CVF authority for prior closures and claim boundaries. |
| `.private_reference/legacy/**` raw files | Do not require broad new legacy scan in this rebuttal. If a raw legacy source is necessary, return `REWRITE_REQUIRED` and name the exact source family and reason. |
| Secrets, provider keys, private raw provenance dumps | Forbidden. Do not request or expose. |

## Non-Authority Surfaces

| Surface | Allowed use | Must not be used for |
| --- | --- | --- |
| `https://github.com/colbymchenry/codegraph` | External product capability and risk input | CVF readiness proof, CVF performance claim, CVF runtime authority |
| `CodeGraph/CVF_Code_Intelligence_Capability/` | External-agent proposal/scaffold to audit | Native CVF source, automatic merge input, dispatch-ready implementation |
| Chat summary of prior CodeGraph audit | Orientation only | Closure evidence or source authority |
| Provider-local memory files | Local operating aid only | CVF source authority table rows |

## Upstream CodeGraph Observations To Challenge

Observed on GitHub on 2026-06-20:

- CodeGraph presents itself as semantic code intelligence for agents including
  Claude Code, Cursor, Codex, Gemini, and others.
- It advertises savings such as lower cost, fewer tokens, faster responses, and
  fewer tool calls. These are upstream claims only and cannot become CVF public
  claims without CVF-specific benchmark proof.
- Its installer can auto-configure agents by wiring the CodeGraph MCP server.
  This is useful capability evidence but high-risk for CVF because it touches
  agent configuration and MCP routing.
- `codegraph init` creates a local `.codegraph/` index and builds a graph.
- Auto-sync/watch behavior is enabled by default in upstream descriptions.
  CVF must not adopt watcher/daemon behavior without a separate work order.
- It exposes graph-style tools such as explore, node, search, callers, callees,
  impact, files, and status.
- It claims local SQLite storage and a local-first/no-code-upload posture.
  Telemetry is still a separate boundary to evaluate; local-first does not
  equal CVF-governed.

Claude should challenge whether any of these observations change the roadmap
disposition below.

## Local CodeGraph Package Findings To Challenge

Local package root:
`CodeGraph/CVF_Code_Intelligence_Capability/`.

Codex preliminary findings:

| Finding | Evidence path | Current Codex disposition |
| --- | --- | --- |
| Package correctly says CVF remains root and graph is not source of truth | `CodeGraph/CVF_Code_Intelligence_Capability/README.md` | ACCEPT_AS_DOCTRINE |
| Package defines graph receipt, stale-index, work-order, review, freeze, and benchmark plans | `CodeGraph/CVF_Code_Intelligence_Capability/docs/reference/` and `docs/protocols/` | ADAPT |
| Package source is mostly scaffold/stub, not running integration | `tools/code-intelligence/*.ts`; `scripts/code-intelligence/*.ts`; `.github/workflows/code-intelligence-check.yml` | DEFER_RUNTIME |
| Package lacks obvious `package.json` or test runner config in the capability folder | `CodeGraph/.../code-intelligence capability folder` | BLOCK_AS_PROOF |
| `GraphReceiptService` can compute `freezeAllowed=true` from graph freshness/confidence alone | `CodeGraph/.../graph-receipt-service.ts` | BLOCKER_FINDING |
| Protocol says graph receipt alone cannot authorize Freeze | `CodeGraph/CVF_Code_Intelligence_Capability/docs/reference/CVF_GRAPH_CONTEXT_RECEIPT_PROTOCOL_2026-06-19.md`; `docs/protocols/CVF_CODE_INTELLIGENCE_FREEZE_PROTOCOL.md` | CVF-aligned doctrine |
| Docs use `snake_case` while schemas/source use `camelCase` | protocol docs vs JSON schemas/source | NORMALIZATION_REQUIRED |
| Local package proposes bridge files under existing LPF graph paths | `CodeGraph/.../copied learning-plane graph files` | DO_NOT_COPY_DIRECTLY |

Claude must verify whether these findings are accurate and whether any
additional blocker exists.

## Current CVF Graph And Legacy-Relevant Authority Surfaces

| Surface | Role | Required Claude check |
| --- | --- | --- |
| `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/knowledge/graph/schema/graph-schema.ts` | Current CVF graph schema and `GraphKnowledgeService` owner | Does CodeGraph roadmap map to this instead of creating a parallel graph core? |
| `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/knowledge/graph/index/symbol-index.ts` | Current CVF symbol index and graph impact path | Does local package duplicate or improve it? |
| `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/knowledge/graph/storage/graph-sqlite-store.ts` | Current optional SQLite persistence | Does upstream/local package add value beyond current PBR04 closure? |
| `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/context_builder/graph/task-query-mapper.ts` | Current task-to-query and blast-radius mapping | Is CodeGraph "impact radius" already partially absorbed here? |
| `EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION/src/knowledge/graph/authority/graph-authority-gate.ts` | Current advisory graph authority gate | Does CGE need to extend this or only document receipt semantics? |
| `docs/reviews/archive/CVF_AIF_B_GRAPH_KNOWLEDGE_PHASE1_COMPLETION_2026-05-24.md` | Graph Phase 1 closure evidence | Does roadmap understate current CVF graph maturity? |
| `docs/reviews/archive/CVF_PBR04_GRAPH_SQLITE_PERSIST_COMPLETION_2026-05-24.md` | Optional SQLite persistence closure | Does roadmap falsely treat durable graph store as missing? |
| `docs/reviews/archive/CVF_N6_AIF_GRAPH_SEARCH_ACTIVATION_COMPLETION_2026-05-24.md` | Advisory `graph_search` activation closure | Does roadmap need to route code-intelligence evidence through this boundary? |
| `docs/reference/archive/CVF_LHW13_T3_GRAPH_CONTEXT_RESOLVER_BOUNDARY_CONNECTOR_SPEC_2026-05-29.md` | Current `interface_only` / `text_retrieval` graph context boundary | Does roadmap preserve this current-state claim? |
| `docs/reference/CVF_MEMORY_DERIVED_GRAPH_BOUNDARY_2026-06-01.md` | Source authority vs derived graph boundary | Does roadmap preserve graph-as-derived/advisory only? |
| `docs/reference/CVF_KGR_ABSORPTION_PREREVIEW_2026-06-01.md` | Graphify/KGR pre-absorption analysis | Is CodeGraph overlapping KGR, or should KGR be a dependency/parallel source? |
| `docs/work_orders/CVF_WO_MKG1_MEMORY_KNOWLEDGE_GRAPH_OWNER_SURFACE_REVIEW_2026-06-01.md` | Prior 47-asset Memory/Knowledge/Graph owner-surface review | Does roadmap need to cite MKG1 before claiming legacy coverage? |

## Proposed CGE Roadmap For Rebuttal

### CGE-T0 - Claude Rebuttal Packet

Status: this packet.

Goal: adversarially review whether CGE should proceed, be rewritten, or be
blocked.

No implementation is authorized.

### CGE-T1 - CodeGraph Absorption Triage Matrix

Goal: create a governed absorption matrix over upstream CodeGraph, local
`CodeGraph/`, and already-known CVF/legacy graph closures.

Expected rows:

| Candidate value | Initial disposition |
| --- | --- |
| Impact radius / callers / callees / route/test surface vocabulary | `ADAPT_TO_EXISTING_LPF_GRAPH` |
| Graph context receipt protocol | `ADAPT_AFTER_FIELD_NORMALIZATION` |
| Stale-index warning and fallback discipline | `ABSORB_AS_GOVERNANCE_RULE` |
| Work Order and Review graph query plan sections | `ABSORB_AS_TEMPLATE_CANDIDATE` |
| Benchmark discipline for file reads/tool calls/tokens | `ABSORB_AS_BENCHMARK_PLAN_ONLY` |
| Upstream installer/agent auto-config | `REJECT_DIRECT` |
| Upstream watcher/daemon auto-sync | `DEFER_REQUIRES_RUNTIME_GC018` |
| Upstream MCP server wiring | `DEFER_REQUIRES_MCP_BOUNDARY_WORK_ORDER` |
| Any token/cost/public performance claim | `BLOCK_UNTIL_CVF_BENCHMARK` |
| Any graph-only freeze authorization | `BLOCK` |

Claude should challenge this table and add missing rows.

### CGE-T2 - Contract And Field Normalization

Goal: if T1 is approved later, normalize graph query/result/receipt fields
before implementation.

Questions for Claude:

- Should CVF standardize receipt fields as `camelCase` to match current TS/JSON
  schemas, or `snake_case` to match prose templates?
- Should `freezeAllowed` be removed, renamed, or forced false at receipt level?
- Should graph receipt output become a new contract, or extend
  `GraphAuthorityReceipt`?
- Which fields must be source-verified from current CVF code versus introduced
  as doc-only new fields?

### CGE-T3 - Existing LPF Graph Reconciliation

Goal: avoid creating a parallel graph subsystem.

Questions for Claude:

- Does `CVF_CODE_INTELLIGENCE_CAPABILITY` duplicate current
  `GraphKnowledgeService`, `SymbolIndex`, `GraphSQLiteStore`, and
  `GraphAuthorityGate`?
- Which local package concepts should become reference/docs only?
- Which concepts deserve direct mapping into existing LPF owners?
- Should any CodeGraph source be rejected because current CVF already closed an
  equivalent bounded capability?

### CGE-T4 - Checker And Template Candidate Planning

Goal: plan only, not implement.

Candidate checks:

- reject graph receipts that claim trusted freeze without review/fallback/test
  evidence;
- require graph-query scope boundary in graph-assisted work orders;
- require stale-index resolution or direct-read fallback before review/freeze
  claims;
- prevent graph impact radius from automatically expanding allowed work-order
  scope;
- prevent public token/cost claims unless benchmark evidence exists.

Claude should rank which checker/template candidate is highest value.

### CGE-T5 - CVF-Specific Benchmark Design

Goal: define benchmark shape only.

Benchmark lanes:

- baseline agent with normal search/read;
- graph-assisted context without CVF receipt enforcement;
- graph-assisted context plus CVF Work Order, receipt, review, and stale-index
  gates.

Metrics:

- file reads;
- tool calls;
- changed files outside scope;
- missed affected files;
- relevant tests identified;
- tests run or justified not run;
- review findings;
- closure pass/fail;
- token/cost/time only as private benchmark fields until public export is
  separately authorized.

No benchmark execution is authorized in this roadmap.

## ACE-R1 Parking Rule

`ACE-R1` remains parked.

Claude should evaluate whether CGE-T1 through CGE-T4 are prerequisites or
useful feeders for ACE-R1. Claude should not recommend reopening ACE-R1 unless
it explains:

1. which CGE findings are already closed enough;
2. what source verification ACE-R1 would need;
3. why ACE-R1 is higher leverage than completing CGE absorption first.

## Knowledge Absorption Blind-Spot Notes

This packet does not claim a complete raw Legacy scan.

Known relevant governed surfaces already point to graph/knowledge absorption
history, including AIF-B, PBR04, N6, LHW13, MKG1, MKG7-T6, and KGR pre-review.

Claude must decide whether those governed surfaces are sufficient for CGE-T0
rebuttal. If Claude believes raw legacy material must be read before CGE-T1,
it must return:

```text
REWRITE_REQUIRED
missingLegacySource: <exact source family/path>
reason: <why current governed closure artifacts are insufficient>
riskIfSkipped: <specific risk>
```

Do not silently open broad legacy absorption in the rebuttal.

## Required Claude Review Questions

1. Is the CGE roadmap correctly sequenced as doctrine/governance first, with
   implementation and runtime wiring deferred?
2. Does the roadmap preserve CVF as root authority and treat CodeGraph/local
   package material as advisory?
3. Does the roadmap understate, overstate, or duplicate current CVF graph
   foundation (`GraphKnowledgeService`, symbol index, SQLite persistence,
   graph_search, authority gate)?
4. Is `freezeAllowed` in the local package a blocker? What exact correction
   should CVF require before any absorb/adapt disposition?
5. Are there missing Legacy/KGR/MKG/AIF/PBR/N6/LHW13 sources that must be
   included before CGE-T1?
6. Which CodeGraph upstream features should be accepted, adapted, deferred,
   rejected, or blocked?
7. Is CGE-T1 the right next governed step, or should CVF open a different
   graph/knowledge absorption lane?
8. What is the smallest useful proof after CGE-T1, assuming no runtime,
   provider/live, public-sync, or benchmark execution?
9. What checker/template candidate would most reduce future agent coding
   evidence risk?
10. Does anything in this packet accidentally authorize CodeGraph install,
   MCP wiring, watcher/daemon behavior, runtime action, public claim, or
   ACE-R1 reopening?

## Expected Claude Output

Return one of:

```text
APPROVE_WITH_FINDINGS
REWRITE_REQUIRED
BLOCK
```

Then include:

| Section | Required content |
| --- | --- |
| Executive decision | One paragraph with chosen disposition |
| Blocking findings | Ordered by severity; use `None` if none |
| Non-blocking findings | Useful improvements |
| Source coverage verdict | Whether the provided sources are enough for CGE-T1 |
| Legacy/KGR coverage verdict | Whether additional legacy source is required |
| Disposition table corrections | Rows to add/change in CGE-T1 |
| Roadmap tranche corrections | Changes to T1-T5 sequence |
| ACE-R1 relation | Keep parked / reopen later / dependency explanation |
| Claim-boundary audit | Any accidental overclaim found |
| Final recommendation | Exact next step for Codex |

## Returned-Output Absorption Reminder

Claude's return remains advisory.

Codex must classify Claude's returned observations through
`docs/reference/external_agent_review/CVF_EXTERNAL_AGENT_FINDING_ABSORPTION_WORKFLOW.md`
before opening CGE-T1, editing any roadmap, creating a work order, or changing
session state.

## Claim Boundary

This packet is a review-context artifact only. It does not:

- authorize CodeGraph install, `codegraph init`, `codegraph install`, npm
  install, or upstream dependency use;
- authorize MCP server wiring, agent auto-config, watcher/daemon behavior,
  `.codegraph/` index creation, or runtime graph execution;
- authorize source/runtime/test edits;
- authorize provider/live proof, secrets/quota use, benchmark execution, or
  public-sync;
- claim token/cost/tool-call reduction for CVF;
- claim production/public/release readiness;
- claim direct IDE/shell/git/filesystem interception;
- claim universal governed-coding control;
- reopen ACE-R1.

## Public Export Disposition

DEFERRED_PRIVATE_ONLY

Reason: private provenance external absorption review packet. No public-sync
batch is authorized.

## Finding-To-Governance Learning Disposition

- Defect class: `ORCHESTRATOR_PACKET_GAP`
- Learning lane: `GOVERNANCE_CONTROL_PLANE`
- Disposition: `DESIGN_REVIEW_REQUIRED`
- Next action: carry accepted blocker rows into CGE-T1 triage matrix.
- Runtime/provider/cost learning lane: `N/A_WITH_REASON` - runtime, provider,
  and cost claims are explicitly blocked until separate CVF proof.

| Finding or lesson | Disposition | Reason |
|---|---|---|
| CodeGraph absorption needs row-level blocker/adapt/defer/reject classification | GOVERNANCE_LEARNING_RETAINED_FOR_CGE_T1 | This packet feeds CGE-T1 triage; no machine-check promotion in T0. |
| External repo/copied-folder input must stay non-canonical until mapped to CVF owners | ALREADY_COVERED_BY_EXTERNAL_KNOWLEDGE_CHAIN | The chain map and routing guard already cover this input type. |

## Delta Execution Claim Boundary Control Block

| Field | Disposition |
|---|---|
| claimScope | CodeGraph external absorption roadmap/rebuttal packet only |
| claimDisposition | N/A with reason: packet rejects runtime/direct-interception/universal-control claims |
| receiptEvidence | N/A with reason: no Delta receipt/action evidence is used |
| actionEvidence | N/A with reason: no execution action is claimed |
| invocationBoundary | no wrapper/proxy, mandatory invocation, direct IDE/shell/git/filesystem interception, or arbitrary command execution |
| interceptionBoundary | no direct interception claim |
| claimLanguage | external absorption planning only |
| forbiddenExpansion | no runtime, MCP, benchmark, provider/live, public-sync, readiness, or universal governed-coding-control claim |

## Rescan Intelligence Hardening

- Original source artifact:
  `https://github.com/colbymchenry/codegraph` and local copied folder
  `CodeGraph/CVF_Code_Intelligence_Capability/`.
- Predecessor intake artifact:
  operator-provided CodeGraph folder and CGE-T0 Codex review packet.
- Delta ledger status: `CHANGED_DISPOSITION` because the input is routed from
  external observation to rebuttal and CGE-T1 decision.
- Routing matrix status:
  - `DO_NOW`: prepare Claude rebuttal packet.
  - `RESOLVED_BY_DESIGN`: use external knowledge routing and absorption table.
  - `SEPARATE_RUNTIME_TRANCHE`: any implementation, MCP, watcher, benchmark, or
    provider/live proof.
  - `STRATEGIC_OPERATOR_DECISION`: whether to open CGE-T1.
  - `OUT_OF_SCOPE`: public-sync, runtime/source mutation, direct interception,
    ACE-R1 reopening.
- Semantic sampling status: `PARTIAL_TARGETED`, limited to CodeGraph absorption
  claim boundary and T1 readiness.
- Rescan intelligence verdict: COMPLETE_WITH_DELTA_ROUTING_SAMPLE

### Original-Intake Delta Ledger

| Delta category | Disposition |
|---|---|
| UNCHANGED_FROM_INTAKE | CodeGraph remains external/advisory input. |
| CHANGED_DISPOSITION | T0 routes the input toward CGE-T1 triage, not implementation. |
| NEW_FINDING | Potential `freezeAllowed` authority leak and parallel LPF graph-core risk require T1 rows. |
| REMOVED_OR_REJECTED | Runtime/source/MCP/public-sync/provider/live work remains out of scope. |

### Follow-Up Routing Matrix

| Lane | Disposition |
|---|---|
| RESOLVED_BY_DESIGN | T0 creates a rebuttal packet and operator review point. |
| DO_NOW | CGE-T1 triage matrix only after operator approval. |
| SEPARATE_RUNTIME_TRANCHE | Any implementation, MCP, watcher/daemon, benchmark, or provider/live proof. |
| STRATEGIC_OPERATOR_DECISION | Operator decides whether to open CGE-T1. |
| DEFER | Any implementation, benchmark, MCP, watcher, or ACE-R1 relation. |
| OUT_OF_SCOPE | Public-sync, runtime execution, provider/live proof, direct interception. |

### Semantic Sampling / Adversarial Review

| sampleId | source section | source claim | disposition checked | adversarial challenge | verdict |
|---|---|---|---|---|---|
| CGE-T0-RS1 | Claim Boundary | packet is planning/rebuttal only | `OUT_OF_SCOPE` | Could the packet be misread as CodeGraph implementation authorization? | PASS_BOUNDARY |
| CGE-T0-RS2 | Required Absorption Table | performance claims need CVF proof | `SEPARATE_RUNTIME_TRANCHE` | Could upstream performance claims be treated as CVF proof? | PASS_BLOCKED |
| CGE-T0-RS3 | External Knowledge Intake Routing | copied package remains advisory | `RESOLVED_BY_DESIGN` | Could copied local package files become canonical by presence alone? | PASS_ROUTED |

## Epistemic Process Block

Expected Result / Prediction: the packet should route CodeGraph as external
advisory input and require Claude rebuttal before any CGE-T1 decision.

Evidence Comparison: Claude rebuttal and Codex classification confirmed the
needed blocker rows: `freezeAllowed` `BLOCK`, LPF-like parallel graph-core
rejection, KGR dedupe, and benchmark gating.

Contradiction Or Gap Disposition: no contradiction invalidated the routing. The
later guard findings were packet-structure defects repaired in this reviewer
batch, not changes to the external absorption claim.

Claim Update: claim remains narrowed to T0 rebuttal preparation only; no
implementation, runtime, public-sync, provider/live, benchmark, ACE-R1, or
universal governed-coding-control claim is made.

Reason: private provenance review-context packet for Claude rebuttal. No
public-sync remote, public commit, public artifact path, or public claim is
authorized.

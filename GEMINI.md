# CVF Gemini Startup Instructions

Before material governed work in this repository, read `CVF_SESSION_MEMORY.md`,
then resolve `CVF_SESSION/ACTIVE_SESSION_STATE.json`, then identify the active
handoff named by that registry.

State or record one concise startup acknowledgment before roadmap,
implementation, review, live run, commit, handoff, or public-sync work:

`Startup acknowledged: current mode=<mode>; active handoff=<handoff>; next allowed move=<summary>; parked checkpoint=<none|summary>.`

Respect these boundaries:

- This is a soft-accountability startup rule, not proof of hard auto-load.
- Do not treat archived handoffs as current unless the state registry points to
  them.
- Do not claim MCP, provider/runtime behavior, hosted readiness, production
  readiness, public-sync, or freeze release from this file alone.
- If the work touches knowledge absorption, memory, graph, intelligence,
  provider/API live runs, or public claims, follow the mandatory standards in
  `AGENTS.md`.


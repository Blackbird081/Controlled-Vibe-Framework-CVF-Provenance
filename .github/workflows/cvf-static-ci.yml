name: CVF Static CI Gate

on:
  push:
    branches: [main, cvf-next]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-ci-gate:
    name: Static CI Gate (no provider secrets)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before || 'HEAD' }}
      HEAD_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: EXTENSIONS/CVF_v1.6_AGENT_PLATFORM/cvf-web/package-lock.json

      - name: Install Control Plane Foundation dependencies
        working-directory: EXTENSIONS/CVF_CONTROL_PLANE_FOUNDATION
        run: npm ci

      - name: Install Model Gateway dependencies
        working-directory: EXTENSIONS/CVF_MODEL_GATEWAY
        run: npm install --no-audit --no-fund

      - name: Install Learning Plane Foundation dependencies
        working-directory: EXTENSIONS/CVF_LEARNING_PLANE_FOUNDATION
        run: npm ci

      - name: Install CVF Guard Contract dependencies
        working-directory: EXTENSIONS/CVF_GUARD_CONTRACT
        run: npm ci

      - name: Install SOT3 package dependencies
        run: |
          npm ci --prefix EXTENSIONS/CVF_TRUTH_KERNEL
          npm ci --prefix EXTENSIONS/CVF_TRUTH_FLOW
          npm ci --prefix EXTENSIONS/CVF_REFINERY

      - name: Install Web dependencies
        working-directory: EXTENSIONS/CVF_v1.6_AGENT_PLATFORM/cvf-web
        run: npm ci

      - name: Run static CI gate
        run: python scripts/run_cvf_static_ci_gate.py --json
